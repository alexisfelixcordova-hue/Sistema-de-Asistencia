{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "curl -O https://archive.apache.org/dist/tomcat/tomcat-9/v9.0.85/bin/apache-tomcat-9.0.85.tar.gz && tar -xzf apache-tomcat-9.0.85.tar.gz && rm -rf apache-tomcat-9.0.85/webapps/ROOT && cp target/*.war apache-tomcat-9.0.85/webapps/ROOT.war && ./apache-tomcat-9.0.85/bin/catalina.sh run",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}